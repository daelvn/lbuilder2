-- lbuilder | stream.moon testing
-- By daelvn

describe "Streams", ->
  it "creates a new stream #new", -> pending "todo"
  it "reads from a stream #read", -> pending "todo"
  it "consumes a stream #consume", -> pending "todo"
  it "moves the pointer in a stream #move", -> pending "todo"
